DB_NAME=your_database_name
DB_USER=your_database_user
DB_PASS=your_database_password
DB_HOST=localhost
PORT=3000

# Create a local .env file (not committed) with real credentials:
# cp .env.example .env && edit .env